within ModelicaAssessment;
package Types
  type GeomInitTypes = enumeration(
      Area "Pipe inner area as input",
      Radius "Pipe inner radius as input",
      Diameter "Pipe inner diamter as input")
    "Enumeration to define the choice of pipe geometry specification";
end Types;
